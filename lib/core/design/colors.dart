import 'package:flutter/material.dart';

class TomeColors {
  // Primary (The Magic)
  static const mysticIndigo = Color(0xFF4F46E5);
  static const etherealPurple = Color(0xFF818CF8);
  static const cyanLight = Color(0xFF22D3EE);

  // Neutral (The Paper)
  static const inkBlack = Color(0xFF0F172A);
  static const slateGrey = Color(0xFF64748B);
  static const washWhite = Color(0xFFF8FAFC);
  static const pureWhite = Color(0xFFFFFFFF);

  // Semantic
  static const error = Color(0xFFEF4444);
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);

  // Dark Mode overrides
  static const darkBackground = Color(0xFF020617);
  static const darkSurface = Color(0xFF1E293B);
  static const darkText = Color(0xFFF1F5F9);
}
